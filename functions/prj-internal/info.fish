set -g SHORTNAME {+basename+}
set -g LONGNAME "{}" # these empty ones are replaced manually
set -g DESCRIPTION "{}"
set -g LOCATION {+location+} 

set -g LABEL_1 " Compile "
# set -g LABEL_2 "a nother displayer"
