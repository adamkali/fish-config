set -g SHORTNAME "nvim"
set -g LONGNAME " Neovim" # these empty ones are replaced manually
set -g DESCRIPTION "Neovim project"
set -g LOCATION "$HOME/.config/nvim" 

set -g LABEL_1 ""
set -g LABEL_2 ""
set -g LABEL_3 ""
set -g LABEL_4 "🤖"
set -g LABEL_5 "󰈺"

set -g LOCATION_NEOVIM          "$HOME/.config/nvim"
set -g LOCATION_LITERATE_CONFIG "$HOME/org/config/nvim"
set -g LOCATION_VS_PLUGIN       "$HOME/projects/vs-solution-treesitter"

set -g ACTION_1 "$EDITOR" 
set -g ACTION_2 "$EDITOR"
set -g ACTION_3 "$EDITOR"
set -g ACTION_4 "claude"
set -g ACTION_5 "$EDITOR"
