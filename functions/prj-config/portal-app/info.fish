set -g SHORTNAME "portal-app"
set -g LONGNAME " eFile Portal" # these empty ones are replaced manually
set -g DESCRIPTION "eFileMadeEasy React v5 Portal."
set -g LOCATION "$HOME/efme/portal/app" 

set -g LABEL_1 ""
set -g LABEL_2 "🤖"
set -g LABEL_3 "󰈺"
set -g LABEL_4 ""
set -g LABEL_5 ""

set -g LOCATION_NOTES           "$HOME/org/efme/eFileMadeEasy.FilingPortal/index.norg"

set -g ACTION_1 "$EDITOR" 
set -g ACTION_2 "claude"
set -g ACTION_4 ""
