set -g SHORTNAME mindscape
set -g LONGNAME "󰧑 Mindscape"
set -g DESCRIPTION "A My homeserver's home page that is a work in progress."
set -g LOCATION $HOME/projects/mindscape/
set -g ORGFILE $HOME/org/mindscape/index.norg
set -g LOCAL $LONGNAME

set -g LABEL_1 " Compile "
set -g LABEL_2 " API     "
set -g LABEL_3 " Web     "
set -g LABEL_4 " Notes   "
set -g LABEL_5 " Claude  "
set -g LABEL_6 "󰈺 Shell   "

